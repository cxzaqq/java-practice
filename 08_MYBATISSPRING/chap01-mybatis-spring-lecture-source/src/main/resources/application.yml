spring:
  datasource:
#    driver-class-name: org.mariadb.jdbc.Driver
#    jdbc-url: jdbc:mariadb://localhost:3307/menudb
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    jdbc-url: jdbc:log4jdbc:mariadb://localhost:3307/menudb
    username: swcamp
    password: swcamp

# fatal -> error -> warn -> info -> debug -> trace
# fatal: ??????? ???? ?? ?? ??? ??
# error: ?? ?? ? ?? ??
# warn: ?? ????? ?? ??? ??? ?? ???
# ?? ???, ?? ?? ? ? ?? ?? ??
# info: ???, ?? ?? ?? ??? ???
# debug: ?? ? ??? ??? ???? ???
# trace: ??? ??? ???? ?? ? ? ??? ??
logging:
  level:
#    root: debug

    com:
      ohgiraffers:
        mybatisspring:
          section01:
            factorybean: debug