server:
  port: 8761

eureka:
  client:
    # eureka server는 eureka server에 등록할 필요가 없다
    register-with-eureka: false
    # eureka server에 등록된 microservice를 주기적으로(30초) 상태 체크할 필요 없다.
    fetch-registry: false